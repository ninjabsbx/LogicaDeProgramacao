using System.ComponentModel.Design;

int X = int.Parse(Console.ReadLine());
int Y = int.Parse(Console.ReadLine());
for (int n = 1; n <= X; n += 2)
{
    Console.WriteLine(n);
}
while for (int n = 1; n <= Y; n += 2)
    {
        Console.WriteLine(n);
    }

